# 隐私说明

KSFAssistant 是本地桌面应用，不提供项目自有账号、云端同步、分析埋点或广告服务。

## 本地处理的数据

- Codex：读取额度、用量及顶层任务状态；本机仅保存归一化额度快照和聚合 Token 统计，不保存对话正文。
- KSF（可选）：只读取用户明确选择目录中的版本化 Catalog/Projection 接口。未配置或失效只关闭项目工作台。
- 飞书（可选）：凭据进入当前用户 Keychain/DPAPI；桥在私有数据目录保存非秘密设置、授权别名、队列状态和审计。KSFAssistant 界面只接收脱敏状态，不接收 Secret、token 或真实飞书 ID。

## 网络连接

应用按用户启用的能力连接 OpenAI/Codex 与飞书/Lark 官方服务。项目不运行额外遥测端点。安装包构建阶段可下载固定版本依赖；普通用户运行时不下载 Node、Go、飞书桥或 lark-cli 组件。

## 外部写入

飞书真实测试发送、任务连接和其他写操作需要用户明确动作。飞书官方管理员授权与发布确认发生在飞书页面，客户端只能引导和核验，不能代替管理员决定。

## 删除

卸载应用不会默认删除用户配置，以便升级和恢复。用户可通过未来的软件内“重置集成”功能清理集成数据；该入口交付前，预览版不会宣称已提供一键删除。请勿在公开问题报告中上传私有数据目录。
