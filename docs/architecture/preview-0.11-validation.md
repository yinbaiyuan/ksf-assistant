# 0.11.0-preview.1 本机预览验收记录（历史检查点）

本文保留 preview.1 的实际观测，不代表 preview.2 用户身份批准门禁已通过验收。新门禁的边界见 [用户身份写操作桌面批准门禁](user-write-approval.md)。

记录日期：2026-09-06。**状态：自动化通过，候选已本机安装；真实卡片闭环存在阻断，未验收完成，不得据此公开发布。**

## 基线与范围

- 代码仓库起点为 `502f007a775634e6fa820eb9c66cfdd4c228d23d`，开始时工作树干净。当前交付包含未提交修改，不使用 HEAD 源码归档冒充本次实现。
- KSF 原有未提交飞书 Skill、项目文档、索引和其他修改保留。本次只增加任务报告 trial Skill、路由 Skill 可选上报段、运行清单/索引入口和数据目录 Git 忽略；不修改类别、岗位、基本功状态或新治理方向。
- 实验台源码、锁文件、资产、测试及专属两篇文档已从工作树移除，无有效入口/引用；执行前差异、源码与退役缓存仍保留在工程外的回退基线中。
- 未暂存、提交、推送、公证或公开发布。冻结包属于本机候选，不等于已安装或已获验收。

## 已执行自动化

| 检查 | 结果 |
| --- | --- |
| `go test ./...` | 通过全部 Go 包 |
| 任务运行、工具链、飞书、integration、IPC、service、rpc、兼容 CLI 和桥入口 `-race` | 通过 |
| `go vet ./...` | 通过；修正新增测试使用 Go 1.24 才有的 testing.Context，保持模块 Go 1.23 |
| `bash scripts/run-tests.sh` | 通过 |
| 冻结 Node 回放 | 284/284 |
| Windows Node 回归 | 76/76 |
| Swift XCTest | 46/46，未启用已退役 standalone 兜底 |
| 身份迁移、Core 管道及 AppKit 显式退出专项 | 通过，使用隔离目录/模拟对象 |
| 四目标 Go 编译 | darwin-arm64、darwin-amd64、windows-amd64、windows-arm64 通过 |
| macOS universal2 与 adhoc 验签 | 主应用及任务/工具链入口双架构通过 |
| 隔离安装与原子升级 | 临时 Applications 目录连续安装两次通过，旧包保留；修正 lipo 参数顺序。首次本机尝试在停止旧应用前被校验拦截，未切换运行版本 |
| 产品身份、发布卫生和 diff whitespace | 通过 |
| 固定 CLI 本地 schema | 实际 1.0.93；确认消息 create/reply/get typed 命令缺失，改为固定 CLI API 入口；patch/images/files schema 可用 |
| CLI 事件进程专项 | 自有消费者 stdin 退出、重复启动拒绝、已有外部总线不关闭；Race 通过 |
| CLI 消息/授权假进程专项 | bot 身份、stdin 正文、错误信封、退出码 10、设备凭据不落盘/argv、取消回收通过 |
| 任务运行隔离专项 | Core 关闭、跨进程 CAS、幂等、原子快照/历史、源变化、明确归属、丢失密钥拒绝初始化通过 |
| 当前真实 KSF verifier 的合成工作区测试 | 通过；仅复制当前二进制及机制脚本，未读取生产凭据 |
| KSF `status` / `verify-runtime` | 通过；7 类别、19 岗位、74 基本功；新报告 Skill 保持 trial |

测试期间发现并修正任务记录原子替换读取竞态、全新用户与密钥丢失的区分、固定 CLI 中不存在的消息 typed 命令，以及 primary 事件未就绪时旧快照误报 ready。一次旧卡片重试测试存在 fixture/inFlight 时序波动，后续定向五次、全量和 Race 均通过；不因此宣称已通过真实卡片回放。

## 本机实测检查点

- 当前安装候选的 macOS 短版本为 `0.11.0`、发布版本为 `0.11.0-preview.1`。修正短版本遗漏，并增加跨 Windows 清单、锁文件、Core 及 plist 的一致性检查；构建前拒绝不一致版本。
- 最终安装来源为 `dist/previews/worktree-Mek9TfLl`，冻结 546 个源码文件，源码摘要 `8870b80a175b86cf6abeb21997fbeb3f4c9031067ea4c8d2eece0ca0d9d99fe9`。对应源码归档、逐文件清单、universal2 ZIP 和产物摘要保留。包内验收文档是冻结时点记录，本节为安装后补充观测，不冒充已包含在该源码归档中。
- 原 `0.10.0` 应用保留为同安装目录中的 `KSFAssistant.previous.20260906-101026.96678.app`；第二次切换也保留中间候选。未恢复或复制业务队列，没有覆盖旧凭据。
- 安装器受控停止后，观察到安装目录下主应用、Core、桥及受管 CLI 进程全部退出；此时受管 task CLI 对当前已验真项目任务写入成功，修订号从 1 到 2。首次相同事件重报返回幂等成功。启动后消息与卡片消费者恢复为 connected/running。
- 受管工具链安装成功，29 个 Skills 文件集处于 managed、健康检查通过。专用新 Codex Desktop 任务在其实际 Skills 清单发现 `ksfas`、`lark-shared`、`lark-im`；通过受管入口确认 CLI `1.0.93` 和 task CLI `0.11.0-preview.1`。
- 同一专用任务按官方 Skill 使用受管 CLI 对 `me` 执行一次明确 user 身份的最小只读查询：退出码 0、`ok: true`，恰好一条自身结果。仅记录这些布尔/数量结果，没有回显或存储身份字段与原始响应。
- 经应用桥的权限检查，bot/user 身份远端验证通过；用户完整业务权限仍缺 44 项，不能把登录成功称为全部权限齐备。本轮没有自动扩权。

## 实机阻断与剩余门槛

应用聚合快照中飞书传输 availability 为 ready，但 `taskLinkReady: false`、阻断项为 `businessIntegration`。业务健康报告两条结果未知事件和任务卡片同步失败。两条未知事件的接收时间早于本轮首次应用切换，未清除、改写终态或重放。

只读抽查一条待同步旧连接：其根消息可通过固定 CLI 读取，但实际是用户发送的文本，不是机器人交互卡片。因此它不能作为可写卡片归属证据；未猜测替代消息、重绑链接或强制清除 pending。该抽查不代表其他旧连接已逐一核验。

桌面自动化对菜单栏应用及飞书原生应用多次超时。应用确已启动、CLI 可读状态，但不能把启动成功当作桌面授权页面或卡片交互的可见验收。已请求用户打开飞书设置面板，并等待确认测试单聊；尚未发送专用卡片或对其他任务执行控制动作。

- 桌面可见授权流程、增量授权与所需最小权限验证。
- 两条未知结果的权威核对、旧卡片归属及同步问题的受控处理；不得为了变绿清空记录。
- 原 Desktop 专用任务绑定、续聊、卡片选择、Plan、中断和解除连接。
- 连续三轮真实选择解除 Desktop 等待；重复点击/旧卡片无重复动作。
- 桌面显式退出按钮的真实验收、任务报告重启补读的可见展示，以及实际测试链接的重启恢复。安装器 TERM 退出检查不能替代这些 UI 项。
- Windows 双架构真实机器上的安装、授权及 Desktop 卡片链路。

上述项目必须记录实际观测，不以假 CLI、交叉编译或已发出的消息替代验收。任一核心项缺失，候选保留“未验收”状态。

## 取舍与回退

预览仅接入已有飞书应用，不自动创建；仅自动管理消息/卡片两项消费者，其他冻结事件明确未启用，固定版不支持的 Mail 事件明确报告不支持。详见 [0.11 边界](preview-0.11.md)。

应用切换保留上一版；Skills 使用自身所有权事务及备份，不覆盖用户改动。回退代码仅恢复本轮改动，KSF 回退不得撤销已有用户修改；回退应用不回滚共享工作队列、发送事实或切换后的业务记录。
