# 0.11.0-preview.2 用户身份门禁验收记录

日期：2026-09-06。状态：候选源码已实现；本机人工批准验收尚未完成，不宣称可公开发布。

## 基线

在原有 preview.1 未提交工作树上增量实施，起点 Git HEAD 为 `502f007a775634e6fa820eb9c66cfdd4c228d23d`。执行前在工程外保存代码与 KSF 的差异、代码文件副本及哈希清单；没有通过提交满足打包要求。本轮未修改 KSF 根规则、治理 Skill、官方 lark-* Skills 或共享业务队列；KSF 差异与本轮基线一致。

## 已运行的隔离自动化

- Go 全量测试与 vet 通过；批准状态机、服务、RPC、工具链和命令适配器相关 Race 通过。飞书执行器的 user 写拒绝/批准、queued 拒绝不重放、身份与策略变化、授权执行锁和兼容身份边界使用假 CLI 验证。
- Swift XCTest 56 项通过；含批准 DTO、长文完整性、原生宿主隔离、并发响应分流、超时/取消、背压下超时请求不补发。AppKit 显式退出专项与隔离身份迁移专项通过。
- Windows 原生宿主/客户端相关专项 41 项通过；未调用真实 Electron 批准按钮。Windows 全量中生成资产哈希依赖重新 staging，须以候选构建后的结果为准，不放宽哈希检查。
- 冻结 Node 兼容回放 284 项通过。产品身份、发布卫生与 diff whitespace 检查通过。
- 固定官方 CLI 离线 help 验证全部 38 条 shortcut/typed 审查命令的参数；未调用生产授权、读取凭据或进行远端写入。16 条明确 API 路由另有语义分类测试。

实际冻结产物的源码哈希、四目标编译、universal2 验签、全量回归及安装后观测必须保存为构建目录内的后续验收记录；它们不是本文件在冻结前已经发生的事实。

## 尚待人工验收

测试目标需用户明确指定。读取不弹框；写入拒绝无副作用；用户亲自批准后恰好一次；关闭/超时不执行；应用未运行时 user 写失败；等待批准期间退出安全。自动化不得代点真实批准按钮。

本轮不处理旧未知事件与旧卡片问题，不恢复任何旧队列。Windows 实机未验收，交叉编译不替代真实验证。未完成上述核心人工项前始终保留候选未验收状态。

## 兼容限制

参见 [门禁架构与边界](user-write-approval.md) 和 [受审查命令表](../../Core/internal/usercommand/README.md)。未审查的 user 写操作拒绝，不声称全部官方业务都已适配；权限变更与动态复合动作未取得宽泛批准。文档创建版本与更新正文是不同真实写操作，各自批准；已有事实不自动回滚。

回退只切换保留的前版应用及未被用户修改的受管工具链内容，不覆盖切换后业务记录。未暂存、提交、推送、公证或公开发布。
