# KSFAssistant 0.11.0-preview.10（历史失败候选）

`0.11.0-preview.10` 曾尝试修复扫码入口，但真实入口复测证明它仍会在生成飞书验证链接前失败，不应继续使用：

- 该候选仍向 `config init` 传入不存在的 `--json`，官方 CLI 会在联网前拒绝命令。
- 该候选一度按受管分发版本校验扫码链接，但受管补丁实际明确使用上游协议版本。
- 它补齐了预览源码冻结中的受管补丁目录，并修正了补丁应用位置；这两项构建修复保留到后续版本。

正确的扫码修复见 `0.11.0-preview.11`。
