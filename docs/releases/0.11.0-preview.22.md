# 0.11.0-preview.22

- 修复内部飞书注册链接强制携带 `createOnly=true`，导致官方扫码页面只能新建机器人、无法选择已有机器人的问题。
- Windows 与 macOS 共用的 Core 现在遵循飞书官方 SDK 默认行为：扫码页同时提供新建与选择已有应用；所需权限、事件和卡片回调仍由同一份 addons 契约预填。
- 新增回归检查，禁止后续注册链接重新携带 `createOnly` 参数。
