# 0.11.0-preview.18

- 兼容飞书官方应用注册入口从 `/page/cli` 切换到 `/page/launcher`，恢复首次点击“扫码连接飞书”后的二维码显示。
- 新旧官方入口继续执行严格的 HTTPS、域名、路径、单值用户码和敏感参数校验；权限、事件及卡片回调仍由共享 Go Core 一次性附加，Windows 与 macOS 使用同一业务链路。
- 同步采用官方 `expires_in` 字段控制注册会话有效期，并新增新入口及恶意变体回归测试。
