# 0.11.0-preview.24

- 修复旧版留下的“注销结果待核实”回执在用户重新连接并验证成功后，仍永久隐藏和阻止注销操作的问题。
- 更晚的应用核验、用户授权核验或测试消息成功回执现在会结束旧注销请求的阻塞生命周期，同时保留其历史结果为“已解决”，不会把未知结果误记为注销成功。
- Windows 飞书配置页在应用已配置时始终保留注销入口；动作暂不可用时显示禁用按钮及 Core 给出的原因，避免控制项无提示消失。
- 回执收敛逻辑位于 Windows 与 macOS 共用的 Core；桌面展示改动仅作用于 Windows，macOS 业务链路不变。
