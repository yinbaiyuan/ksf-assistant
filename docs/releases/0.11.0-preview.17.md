# 0.11.0-preview.17

- 飞书任务卡片链路改为产品内置的官方 Go SDK 与受限 OpenAPI，不再运行或随包分发受管 `lark-cli`。
- 一次扫码同时申请任务消息与卡片回调订阅，并把扫码用户直接绑定为当前操作者；Windows 使用 DPAPI、macOS 使用 Keychain 保存应用凭据。
- Windows 与 macOS 安装包、SBOM 和发布检查不再包含飞书 Skills、CLI 清单或 CLI 二进制；原跳过项改为始终执行的内部运行时完整性测试。
- 旧受管凭据只作为一次性本机迁移输入，迁移后运行链路只读取产品自有凭据存储。
