# 0.11.0-preview.23

- 修复应用快速重启时旧飞书 Bridge 尚未退出，新 Core 在有限崩溃重试后永久停留于“无法读取最新运行状态”的问题。
- 监督器现在会在启动前识别仍存活的上一实例，并在五分钟内以独立的两秒节奏等待交接；该等待不消耗普通崩溃重试预算。
- 普通启动失败、初始化失败和崩溃循环仍沿用原有有限重试与降级策略；Windows 与 macOS 共用相同生命周期规则。
